require 'json'

describe User, "#create_or_update" do
  before :each do
    #create an auth hash as it would be returned from facebook
    
    
  end
  it 'should create a new user when none exists' do
  end
end